@page
@model CabukHemenSimdiIzle.MVC.Models.DirectorViewModel

@{
    ViewData["Title"] = "Add Director";
}

@* <div class="container mt-4">
    <h1>@ViewData["Title"]</h1>

    <form asp-action="AddAsync" method="post">
        <div class="form-group">
            <label asp-for="FirstName">First Name:</label>
            <input asp-for="FirstName" class="form-control" required />
            <span asp-validation-for="FirstName" class="text-danger"></span>
        </div>

        <div class="form-group">
            <label asp-for="LastName">Last Name:</label>
            <input asp-for="LastName" class="form-control" required />
            <span asp-validation-for="LastName" class="text-danger"></span>
        </div>

        <div class="form-group">
            <label for="MovieIds">Movie IDs (comma-separated):</label>
            <input type="text" id="MovieIds" name="MovieIds" class="form-control" />
            
        </div>

        <button type="submit" class="btn btn-primary">Add Director</button>
    </form>
</div> *@

