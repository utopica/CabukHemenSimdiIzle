@using CabukHemenSimdiIzle.Domain.Enums;
@model List<MovieViewModel>

<div class="container-fluid">
    <div class="film-card-container">
        @if (Model.Count == 0)
        {
                <h3>There is no movie :(</h3>
        }
        else
        {

            @foreach (var movie in Model)
            {
                <div class="card film-card">
                    <a asp-controller="Home" asp-action="Detail" asp-route-id="@movie.Id">
                        <img src="@movie.PictureUrl.Split("||").FirstOrDefault()" class="card-img-top" alt="@movie.Title">
                        <div class="card-body">
                            <h5 class="card-title">@movie.Title</h5>


                            <h6 class="card-title">@movie.ReleaseDate.ToString()</h6>

                        </div>


                    </a>
                </div>
            }
        }

    </div>
</div>

